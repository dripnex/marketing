Desktop chrome, copied from `dripnex/readide` `apps/desktop/src/renderer`.

These CSS modules and presentational pieces are the product UI — not a restyle.
Do not rewrite class names here. Sync from desktop when the chrome changes.
